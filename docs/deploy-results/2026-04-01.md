# Deploy Result

- 일자별 결과 문서는 `docs/deploy-results/YYYY-MM-DD.md` 형식으로 생성해 보관한다.
- 동일 문서 안에서 `검증 환경 결과` 와 `실제 운영 배포 결과` 를 반드시 분리 기록한다.
- 현재 [docs/12-release-readiness.md](../12-release-readiness.md) 기준 판정이 `READY` 여도, 실제 운영 배포 완료 여부는 이 문서에서 별도로 기록한다.
- 실제 운영 배포를 수행하지 않았다면 관련 항목은 `미실행` 또는 `미기록` 으로 남긴다.

## 1. 기본 정보

- 기준 일자: `2026-04-01`
- release readiness 기준 판정: `READY`
- 배포 버전: `미기록`
- 관련 커밋/브랜치: `9aaa767 (main)`
- 배포 담당자: `Codex 로컬 검증 세션`
- 검수 담당자: `미기록`
- 관련 readiness 문서:
  - [docs/12-release-readiness.md](../12-release-readiness.md)
  - [docs/13-pre-deploy-runbook.md](../13-pre-deploy-runbook.md)
  - [docs/15-go-live-checklist.md](../15-go-live-checklist.md)
- 비고: `docker-compose.yml` 의 `restart: unless-stopped` 와 운영용 `.env` 반영 여부를 로컬 검증 스택에서 실제 재생성/재시작으로 확인한 기록이다. 실제 운영 서버 반영은 수행하지 않았다.

---

## 2. 현재 판정

- readiness 현재 판정: `READY`
- 검증 환경 결과 반영 여부: `반영 완료`
- 실제 운영 배포 진행 여부: `미실행`
- 비고: 이번 기록으로 `운영 배포 마무리 체크 문서/설정 정리` 단계의 로컬 검증 증빙은 `Docker Compose 재기동`, `MariaDB/DB_DUMP`, `custom admin(yjkwak520) 기준 최종 브라우저 검증` 까지 반영 완료로 판정한다. 다만 실제 운영 서버 투입 여부는 별도 판정이 필요하다.

---

## 3. 검증 환경 결과

### 3.1 검증 개요

- 검증 수행 여부: `예`
- 검증 환경: `로컬 Docker Compose 검증 스택`
- 검증 시작 시각: `2026-04-01 01:41:19 +09:00`
- 검증 완료 시각: `2026-04-01 01:44:06 +09:00`
- 검증 요약: `루트 .env 를 임시 생성해 docker compose down/config/up --build --force-recreate/restart 를 순서대로 실행했고, backend/frontend 컨테이너의 restart policy 가 둘 다 unless-stopped 로 반영된 것을 확인했다. 재시작 직후 frontend /api/v1/health 는 502 를 반환했지만 backend healthy 복구 후 같은 endpoint 는 200/UP 으로 정상화됐다.`

### 3.2 MariaDB/MySQL 실검증 결과

- 실행 일시: `2026-04-01`
- 실행 환경: `docker compose backend prod profile`
- 실행 명령: `미실행`
- 결과:
  - [ ] 성공
  - [ ] 실패
  - [x] skip
- 비고: 이번 턴은 `backend/src/main/resources/schema.sql` 의 실제 운영 DB 적용 작업이 범위 밖이었다. 다만 검증 스택은 기존 검증용 MariaDB (`mental_health_prod_verify`) 에 연결되어 `dbStatus=UP` 으로 정상 기동했다.

### 3.3 DB_DUMP 수동 백업 결과

- 실행 일시: `2026-04-01 04:33:38 ~ 04:33:40 +09:00`
- 실행 환경: `로컬 MariaDB 검증 스크립트 (scripts/verify-db-dump-local.ps1, backend local profile @ 18080, MariaDB 12.2.2 @ 3307)`
- 실행 계정: `admina (관리자A)`
- 실행 방식:
  - [ ] 관리자 화면
  - [x] API
- 입력 사유: `배포 직전 수동 백업`
- 응답 상태: `200 / SUCCESS`
- `backupMethod`: `DB_DUMP`
- `datasourceType`: `MARIADB`
- `preflightSummary`: `datasource=MARIADB, preferred=DB_DUMP, dumpCommand=D:\dasiseogi-mental-health-intranet-template\tmp\mariadb\dist\MariaDB-12.2\bin\mariadb-dump.exe, dumpAvailable=true, fallback=-`
- 생성 파일명: `backup-20260401-043338-db-dump.sql`
- 생성 파일경로: `D:\dasiseogi-mental-health-intranet-template\local-backups\backup-20260401-043338-db-dump.sql`
- 파일 존재 확인:
  - [x] 예
  - [ ] 아니오
- `backup_histories` 최신 이력 확인:
  - [x] 예
  - [ ] 아니오
- 비고: `scripts/verify-db-dump-local.ps1` 를 실제 실행해 `healthStatus=UP`, `backupStatus=SUCCESS`, `backupMethod=DB_DUMP`, `datasourceType=MARIADB` 를 확인했다. 같은 백업 건에 대해 백업 API 목록 최신 1건(`backupId=9`)과 직접 SQL 조회 최신 1건(`backup_histories.id=9`)이 모두 `DB_DUMP / SUCCESS` 로 일치했고, `.sql` dump 파일도 실제 경로에 생성됐다. 이전 `SNAPSHOT/H2` 확인은 blocker 해소가 아니었고, 이번 `DB_DUMP/MARIADB` 검증으로 로컬 검증 기준 백업 blocker 를 해소했다.

### 3.4 오늘 검증 체크리스트 결과

- `docker compose down --remove-orphans`
  - [x] 성공
  - [ ] 실패
- 루트 `.env` 임시 준비
  - [x] 성공
  - [ ] 실패
- `docker compose config`
  - [x] 성공
  - [ ] 실패
- `docker compose up -d --build --force-recreate`
  - [x] 성공
  - [ ] 실패
- `docker compose ps`
  - [x] 성공
  - [ ] 실패
- backend/frontend restart policy 확인
  - [x] 성공
  - [ ] 실패
- backend/frontend health 확인
  - [x] 성공
  - [ ] 실패
- 로그인 전 `/api/v1/auth/me` 401 확인
  - [x] 성공
  - [ ] 실패
- 로그인 후 `/api/v1/auth/me` 200 확인
  - [x] 성공
  - [ ] 실패
- custom admin bootstrap 후 로그인 확인
  - [x] 성공
  - [ ] 실패
- 관리자 메뉴 4종 실제 화면 확인
  - [x] 성공
  - [ ] 실패
- 대상자 등록 확인
  - [x] 성공
  - [ ] 실패
- 최소 2종 멀티 척도 세션 저장 확인
  - [x] 성공
  - [ ] 실패
- 세션 상세 확인
  - [x] 성공
  - [ ] 실패
- `print view` 확인
  - [x] 성공
  - [ ] 실패
- `statistics` 3섹션 확인
  - [x] 성공
  - [ ] 실패
- `CSV export` 확인
  - [x] 성공
  - [ ] 실패
- 활동 로그 확인
  - [x] 성공
  - [ ] 실패
- 백업 트리거 미실행 확인
  - [x] 성공
  - [ ] 실패
- 수동 백업 1회 실행
  - [x] 성공
  - [ ] 실패
- 수동 백업 이력 확인
  - [x] 성공
  - [ ] 실패
- `docker compose restart`
  - [x] 성공
  - [ ] 실패
- 재시작 직후 frontend `/api/v1/health` 재확인
  - [x] 성공
  - [ ] 실패
- backend healthy 이후 frontend `/api/v1/health` 재확인
  - [x] 성공
  - [ ] 실패
- 최종 git working tree 확인
  - [x] 성공
  - [ ] 실패
- 메모: 서버 계산/판정 기준, 세션 단위 저장, 삭제 대신 상태값 처리, 권한 분리 기준은 이번 검증에서 변경하지 않았다. 수동 백업은 `scripts/verify-db-dump-local.ps1` 기준 `DB_DUMP/MARIADB` 로 재검증되어 dump 파일 생성, API 이력, `backup_histories` 최신 행까지 확인했다. 추가로 custom admin `yjkwak520` 기준 최종 브라우저 검증에서 관리자 메뉴 4종, 대상자 등록, `PHQ-9 + GAD-7` 세션 저장, 세션 상세, `print view`, `statistics`, `CSV export`, 활동 로그를 실제 화면 기준으로 확인했고, `BACKUP_TRIGGER_ALLOWED=false` 조건에 따라 백업 실행은 수행하지 않았다.

### 3.5 Docker Compose 검증 결과

- 검증 범위:
  - [x] `docker compose config`
  - [x] `docker compose ps`
  - [x] `docker compose logs backend --tail=100`
  - [x] `docker compose logs frontend --tail=100`
  - [x] `docker inspect -f "{{ .HostConfig.RestartPolicy.Name }}" <backend-container>`
  - [x] `docker inspect -f "{{ .HostConfig.RestartPolicy.Name }}" <frontend-container>`
  - [x] `http://127.0.0.1:8080/api/v1/health`
  - [x] `http://127.0.0.1:4173/api/v1/health`
  - [x] `docker compose restart`
  - [x] 재시작 후 `docker compose ps`
  - [x] 재시작 직후 `http://127.0.0.1:4173/api/v1/health`
  - [x] 안정화 후 `http://127.0.0.1:4173/api/v1/health`
- `.env` 준비 방식:
  - `.env.docker.example` 구조를 기준으로 루트 `.env` 를 임시 생성했다.
  - 값은 재생성 전 실행 중이던 backend 컨테이너의 현재 환경값을 기준으로 맞췄다.
  - 검증 종료 후 `.env` 는 삭제해 working tree 에 남기지 않았다.
- `.env` 키 요약:
  - `APP_DB_URL_DOCKER=jdbc:mariadb://host.docker.internal:3306/mental_health_prod_verify?useUnicode=true&characterEncoding=utf8`
  - `APP_DB_USERNAME=m***r`
  - `APP_DB_PASSWORD=***MASKED***`
  - `APP_DB_DRIVER=org.mariadb.jdbc.Driver`
  - `APP_SESSION_TIMEOUT=120m`
  - `APP_FORWARD_HEADERS_STRATEGY=none`
  - `APP_TRUST_PROXY_HEADERS=false`
  - `APP_DB_DUMP_COMMAND=(empty)`
  - `BACKEND_LOGS_HOST_PATH=./logs`
  - `BACKEND_TMP_HOST_PATH=./tmp`
  - `BACKEND_BACKUPS_HOST_PATH=./local-backups`
- `docker compose config` 확인 결과:
  - backend 환경값에 `APP_DB_URL_DOCKER`, `APP_DB_USERNAME`, `APP_DB_PASSWORD`, `APP_DB_DRIVER` 가 채워진 상태로 출력됐다.
  - backend/frontend 모두 `restart: unless-stopped` 로 해석됐다.
  - bind mount 는 `logs`, `tmp`, `local-backups` 로 해석됐다.
- `docker compose up -d --build --force-recreate` 결과:
  - backend/frontend 이미지를 다시 확인한 뒤 컨테이너를 재생성했다.
  - backend 가 `Healthy` 로 바뀐 뒤 frontend 가 시작됐다.
- `docker compose ps` 확인 결과:
  - 재생성 직후: `backend Up (healthy)`, `frontend Up`
  - 재시작 직후: `backend Up (health: starting)`, `frontend Up`
  - 안정화 후: `backend Up (healthy)`, `frontend Up`
- restart policy 확인 결과:
  - backend 컨테이너 `dasiseogi-mental-health-intranet-template-backend-1`: `unless-stopped`
  - frontend 컨테이너 `dasiseogi-mental-health-intranet-template-frontend-1`: `unless-stopped`
- backend 로그 확인 결과:
  - `backend-entrypoint: using external database configuration with profile prod`
  - MariaDB 연결 시작 및 완료 로그 확인
  - `Loaded 9 scale registry items and 9 implemented scale definitions`
  - `Tomcat started on port 8080`
- frontend 로그 확인 결과:
  - nginx entrypoint 초기화 및 worker process 시작 로그 확인
  - 운영 차단 이슈로 볼 만한 오류는 없었다.
- health endpoint 결과:
  - `http://127.0.0.1:8080/api/v1/health`
    - 재생성 후: `200`
    - 응답: `status=UP`, `appStatus=UP`, `dbStatus=UP`, `scaleRegistryStatus=UP`, `loadedScaleCount=9`
  - `http://127.0.0.1:4173/api/v1/health`
    - 재생성 후: `200`
    - 재시작 직후: `502 Bad Gateway`
    - backend healthy 이후 재확인: `200`
    - 최종 응답: `status=UP`, `appStatus=UP`, `dbStatus=UP`, `scaleRegistryStatus=UP`, `loadedScaleCount=9`
- 인증 스모크 결과:
  - 비로그인 상태 `GET /api/v1/auth/me`: `401`
  - 검증용 기존 관리자 계정으로 `POST /api/v1/auth/login`: `200`
  - 로그인 후 `GET /api/v1/auth/me`: `200`
  - 확인된 사용자: `loginId=admina`, `role=ADMIN`, `status=ACTIVE`
  - 주의: 위 로그인 성공은 `mental_health_prod_verify` 검증 DB 에 이미 존재하던 계정 기준이다. 실제 운영 DB 의 초기 관리자 준비 완료를 뜻하지 않는다.
- `schema.sql` 사전 적용 여부: `실제 운영 DB 기준 미확인`
- 초기 관리자 계정 준비 방식: `실제 운영 DB 기준 미실행`
- working tree 정리 결과:
  - 임시 `.env` 삭제 완료
  - 최종 `git status --short` 에서는 이 결과 문서 1건만 변경분으로 남도록 정리했다.

- 실행 명령:

```powershell
docker compose down --remove-orphans
docker compose config
docker compose up -d --build --force-recreate
docker compose ps
docker inspect -f "{{ .HostConfig.RestartPolicy.Name }}" dasiseogi-mental-health-intranet-template-backend-1
docker inspect -f "{{ .HostConfig.RestartPolicy.Name }}" dasiseogi-mental-health-intranet-template-frontend-1
docker compose logs backend --tail=100
docker compose logs frontend --tail=100
Invoke-WebRequest -UseBasicParsing -Uri http://127.0.0.1:8080/api/v1/health
Invoke-WebRequest -UseBasicParsing -Uri http://127.0.0.1:4173/api/v1/health
Invoke-WebRequest -UseBasicParsing -Uri http://127.0.0.1:4173/api/v1/auth/me
Invoke-WebRequest -UseBasicParsing -WebSession $session -Uri http://127.0.0.1:4173/api/v1/auth/login -Method Post -ContentType application/json -Body '{"loginId":"<existing-validation-admin>","password":"***MASKED***"}'
Invoke-WebRequest -UseBasicParsing -WebSession $session -Uri http://127.0.0.1:4173/api/v1/auth/me
docker compose restart
docker compose ps
Invoke-WebRequest -UseBasicParsing -Uri http://127.0.0.1:4173/api/v1/health
docker compose ps
Invoke-WebRequest -UseBasicParsing -Uri http://127.0.0.1:8080/api/v1/health
Invoke-WebRequest -UseBasicParsing -Uri http://127.0.0.1:4173/api/v1/health
git status --short
```

### 3.6 로컬 최종 브라우저 검증 결과

- 실행 일시: `2026-04-01 17:35:33 ~ 17:41:10 +09:00`
- 실행 환경: `로컬 Docker + local MariaDB runtime`, `Windows Chrome headless 실브라우저 렌더`
- 사용 계정: `yjkwak520`
- 검증 기준:
  - seed admin 기준이 아니라 custom admin 기준 로그인 성공 여부를 별도로 확인했다.
  - 서버 계산/판정은 저장 후 서버 응답과 화면 렌더 관찰값만 기록했다.
  - 세션 단위 저장 구조, 삭제 대신 상태값 처리 원칙, 권한 분리 구조는 변경하지 않았다.
- 인증/기본 상태 확인:
  - `/login` 렌더 확인: `PASS`
  - `/api/v1/health`: `200`, `status=UP`, `appStatus=UP`, `dbStatus=UP`, `scaleRegistryStatus=UP`, `loadedScaleCount=9`
  - 로그인 전 `/api/v1/auth/me`: `401`
  - 로그인 후 `/api/v1/auth/me`: `200`
  - 확인된 사용자: `loginId=yjkwak520`, `role=ADMIN`, `status=ACTIVE`
- 관리자 메뉴 실제 화면 확인:
  - `/admin/signup-requests`: `PASS`
  - `/admin/users`: `PASS`
  - `/admin/logs`: `PASS`
  - `/admin/backups`: `PASS`
- 대상자 등록/세션 저장 결과:
  - 신규 대상자 생성: `PASS`
  - 생성 대상자: `WEB_AGENT_SMOKE_20260401_173826`
  - `clientId=7`
  - `clientNo=CL-202604-0004`
  - `PHQ-9 + GAD-7` 멀티 척도 세션 저장: `PASS`
  - 세션 메모 공란 저장: `PASS`
  - `sessionId=2`
  - `sessionNo=AS-20260401-0002`
- 세션 상세/출력 결과:
  - 세션 상세 실제 화면 렌더: `PASS`
  - 서버 계산 결과 관찰값:
    - `PHQ-9 총점 27 / 중증`
    - `GAD-7 총점 21 / 중증`
    - 경고 `3건`
  - `print view` 실제 화면 렌더: `PASS`
  - OS native 인쇄 대화상자 제어: `미확인`
- statistics/CSV/log 결과:
  - `statistics` 실제 화면 렌더: `PASS`
  - 관찰값:
    - `전체 세션 2`
    - `전체 척도 시행 4`
    - `경고 세션 1`
    - `경고 척도 2`
    - alerts 기록 `3건`
  - `CSV export`: `PASS`
  - 파일명: `statistics-summary-2026-03-30-2026-04-05.csv`
  - 파일 크기: `218 bytes`
  - 활동 로그 확인:
    - `LOGIN`
    - `CLIENT_CREATE`
    - `SESSION_CREATE`
    - `PRINT_SESSION`
    - `STATISTICS_EXPORT`
- 백업 관련:
  - 백업 관리 화면 진입: `PASS`
  - 백업 실행: `미실행`
  - 비고: `BACKUP_TRIGGER_ALLOWED=false` 조건에 따라 이번 브라우저 검증에서는 백업 트리거를 수행하지 않았다.
- 최종 판정:
  - 로컬 기준 최종 브라우저 검증: `PASS`
  - 주의: 위 결과는 로컬 검증 스택에서의 custom admin 기준 최종 브라우저 증빙이며, 실제 운영 배포 또는 실제 운영 성공을 의미하지 않는다.

### 3.7 실웹브라우저 기준 전체 척도 커버 검증

#### 실행 일시 / 로컬 환경 전제

- 실행 일시: `2026-04-01 18:58:51 ~ 18:59:22 +09:00`
- 로컬 환경 전제:
  - `http://127.0.0.1:4173`
  - `로컬 Docker + local MariaDB runtime`
  - `Windows Chrome headless 실브라우저 렌더`
- source of truth 확인:
  - `backend/src/main/resources/scales/common/scale-registry.json` 을 먼저 읽고 활성 척도 9종 `PHQ-9`, `GAD-7`, `mKPQ-16`, `K-MDQ`, `PSS-10`, `ISI-K`, `AUDIT-K`, `IES-R`, `정신과적 위기 분류 평정척도 (CRI)` 를 확인했다.
  - 세션 A 척도 선택 화면에서 같은 9종이 모두 `현재 사용 가능` 상태로 실제 렌더되는 것을 브라우저에서 재확인했다.
- 검증 범위 메모:
  - 각 세션마다 `척도 선택 화면 진입 -> 모든 문항 응답 입력 -> 저장 -> 세션 상세 확인 -> print view 확인` 을 실제 브라우저 UI 흐름으로 수행했다.
  - 성공 근거는 API 200 단독이 아니라 저장 후 세션 상세/print view 의 실제 화면 렌더와 서버 계산 결과 관찰값을 기준으로 남겼다.

#### 사용한 admin 계정 출처 설명

- 사용 계정: `yjkwak520`
- 계정 출처 설명:
  - 기존 custom admin bootstrap 결과와 본 문서 `3.6 로컬 최종 브라우저 검증 결과` 의 `loginId=yjkwak520`, `role=ADMIN`, `status=ACTIVE` 기록을 기준으로 계정을 식별했다.
  - 실제 로그인은 같은 로컬 bootstrap 결과의 비공개 비밀번호로 수행했고, 민감정보는 이 문서에 기록하지 않았다.
- 로그인 확인 결과: `loginId=yjkwak520`, `role=ADMIN`, `status=ACTIVE`, `userId=7`, `name=KWAKYOUNGJOON`

#### 사용한 스모크 대상자 정보

- 대상자 사용 방식: `신규 전용 스모크 대상자 생성`
- `CLIENT_CREATE`: `실행`
- 대상자명: `WEB_FULL_SCALE_SMOKE_20260401_185855`
- `clientId=11`
- `clientNo=CL-202604-0008`
- 비고: 이번 턴은 기존 스모크 대상자 재사용이 아니라 신규 스모크 대상자 1명을 생성해 4개 세션을 모두 같은 대상자에 대해 수행했다.

#### 세션 A/B/C/D 별 수행 결과 표

| 세션 | 척도 | 브라우저 입력 패턴 | 저장 결과 | 세션 상세 | print view |
|---|---|---|---|---|---|
| A | `PHQ-9` + `GAD-7` | `PHQ-9=거의 매일 x9`, `GAD-7=거의 매일 x7` | `PASS`, `sessionId=8`, `sessionNo=AS-20260401-0008` | `PASS` | `PASS` |
| B | `mKPQ-16` + `K-MDQ` | `mKPQ-16=예/아니오 교차`, `K-MDQ=예 중심 혼합 응답` | `PASS`, `sessionId=9`, `sessionNo=AS-20260401-0009` | `PASS` | `PASS` |
| C | `PSS-10` + `ISI-K` | `PSS-10=5단계 순환`, `ISI-K=약간/보통/심함 혼합` | `PASS`, `sessionId=10`, `sessionNo=AS-20260401-0010` | `PASS` | `PASS` |
| D | `AUDIT-K` + `IES-R` | `AUDIT-K=저빈도~고위험 혼합`, `IES-R=0~4 순환` | `PASS`, `sessionId=11`, `sessionNo=AS-20260401-0011` | `PASS` | `PASS` |

#### 각 세션의 세션 상세 확인 결과

- 세션 A `AS-20260401-0008`
  - `PHQ-9 총점 27 / 중증`
  - `GAD-7 총점 21 / 중증`
  - 경고 관찰값: `PHQ9 item 9`, `GAD7 CAUTION`, `GAD7 HIGH_RISK`
- 세션 B `AS-20260401-0009`
  - `mKPQ-16 총점 11 / 양성 의심`
  - `K-MDQ 총점 11 / 양성 의심`
  - 경고 관찰값: `MKPQ16 CAUTION`, `KMDQ CAUTION`
- 세션 C `AS-20260401-0010`
  - `PSS-10 총점 16 / 공식 절단점 없음`
  - `ISI-K 총점 13 / 역치하 불면`
  - 경고 관찰값: `없음`
- 세션 D `AS-20260401-0011`
  - `AUDIT-K 총점 21 / 알코올 의존 의심`
  - `IES-R 총점 41 / 고위험 의심`
  - 경고 관찰값: `AUDITK CAUTION/HIGH_RISK 4건`, `IESR CAUTION/HIGH_RISK 2건`

#### 각 세션의 print view 확인 결과

- 세션 A print view: `PASS`
  - `PHQ-9 27 / 중증`
  - `GAD-7 21 / 중증`
- 세션 B print view: `PASS`
  - `mKPQ-16 11 / 양성 의심`
  - `K-MDQ 11 / 양성 의심`
- 세션 C print view: `PASS`
  - `PSS-10 16 / 공식 절단점 없음`
  - `ISI-K 13 / 역치하 불면`
- 세션 D print view: `PASS`
  - `AUDIT-K 21 / 알코올 의존 의심`
  - `IES-R 41 / 고위험 의심`
- 공통 메모: OS native 인쇄 대화상자 자체는 자동 제어 범위 밖이라 `미확인` 으로 남긴다. 이번 PASS 근거는 print route 실제 렌더와 서버 결과 표 확인이다.

#### statistics 확인 결과

- 확인 결과: `PASS`
- 요약 카드 관찰값:
  - `전체 세션 11`
  - `전체 척도 시행 22`
  - `경고 세션 8`
  - `경고 척도 16`
- 현재 운영 척도 반영 확인:
  - `PHQ-9 5 / 경고 4`
  - `GAD-7 5 / 경고 4`
  - `mKPQ-16 2 / 경고 2`
  - `K-MDQ 2 / 경고 2`
  - `PSS-10 2 / 경고 0`
  - `ISI-K 2 / 경고 0`
  - `AUDIT-K 2 / 경고 2`
  - `IES-R 2 / 경고 2`
- 비고: statistics 는 누적 화면이므로 기존 저장분을 포함한다. 다만 이번 실행 후 현재 운영 척도 표에서 활성 9종이 모두 1건 이상으로 반영된 것을 확인했다.

#### CSV export 확인 결과

- 확인 결과: `PASS`
- 실행 화면: `/statistics`
- 실행 버튼: `요약 CSV`
- 파일명: `statistics-summary-2026-03-30-2026-04-05.csv`
- 임시 저장 경로: `C:\Users\kyj\AppData\Local\Temp\codex-pw-win\artifacts-full-scale\statistics-summary-2026-03-30-2026-04-05.csv`
- 파일 생성 여부: `예`
- 빈 파일 아님 여부: `예`
- 파일 크기: `222 bytes`

#### 활동 로그 확인 결과

- 확인 화면: `/admin/logs`
- 확인 조건: `userId=7`, `pageSize=100`, 최신순
- 필수 이벤트 확인:
  - `LOGIN`: `2026-04-01T09:58:54` 행에서 `yjkwak520 (USER #7)` 로그인 성공 확인
  - `CLIENT_CREATE`: `2026-04-01T09:58:55` 행에서 `WEB_FULL_SCALE_SMOKE_20260401_185855 (CLIENT #11)` 확인
  - `SESSION_CREATE`: `AS-20260401-0008`, `AS-20260401-0009`, `AS-20260401-0010`, `AS-20260401-0011` 각 저장 행 확인
  - `PRINT_SESSION`: 위 4개 세션 번호 각각의 출력 데이터 조회 행 확인
  - `STATISTICS_EXPORT`: `2026-04-01T09:59:19` 행에서 `SUMMARY (STATISTICS)` 확인
- 보조 관찰값:
  - action type 필터 없이도 화면 본문에서 최신 관련 행들이 연속으로 보였다.
  - 이번 스모크 대상자/세션 기준 최신 관련 행은 `CLIENT_CREATE 1건`, `SESSION_CREATE 4건`, `PRINT_SESSION 4건` 으로 확인됐다.

#### 백업 미실행 / 미실행 항목

- 백업 실행: `미실행`
- 백업 관련 비고: 이번 작업 범위는 브라우저 기준 전체 척도 커버 검증 1건이므로 백업 트리거는 수행하지 않았다.
- 삭제 테스트: `미실행`
- 대상자 오등록 처리: `미실행`
- 세션 오입력 처리: `미실행`
- OS native 인쇄 대화상자 직접 확인: `미확인`

#### 최종 판정

- 최종 판정: `PASS`
- 판정 근거:
  - 활성 척도 9종을 모두 실제 브라우저에서 최소 1회 이상 사용했다.
  - 세션 A/B/C/D 4개가 모두 저장됐다.
  - 각 세션의 세션 상세 확인과 print view 확인을 모두 완료했다.
  - statistics 누적 반영, CSV export, 활동 로그 필수 이벤트 확인을 모두 완료했다.
- 주의: 위 결과는 `실제 운영 성공이 아니라 로컬 기준 브라우저 검증 결과` 이다.

---

## 4. 실제 운영 배포 결과

- 실제 운영 배포 진행 여부: `미실행`

### 4.1 배포 수행 메타

- 배포 시작 시각: `미실행`
- 배포 완료 시각: `미실행`
- 배포 대상 환경: `미기록`

### 4.2 배포 수행 기록

- 백엔드 반영 결과: `미실행`
- 프론트 반영 결과: `미실행`
- 설정 파일 반영 결과: `미실행`
- 척도 JSON 반영 결과: `미실행`
- 배포 중 오류: `미기록`

### 4.3 배포 후 스모크 테스트 결과

- `health` endpoint 확인: `미실행`
- 기준: `HTTP 200`, `status=UP`, `appStatus=UP`, `dbStatus=UP`, `scaleRegistryStatus=UP`, `loadedScaleCount` 가 기대 척도 수와 일치하는지 실제 운영에서 확인해야 한다.
- 관리자 로그인 확인: `미실행`
- 기준: bootstrap 또는 기존 운영 관리자 계정 로그인 성공을 실제 운영에서 확인해야 한다.
- 관리자 메뉴 접근 확인 / 일반 사용자 계정 관리자 메뉴 미노출 확인: `미실행`
- 기준: 관리자 메뉴 접근 가능 여부와 일반 사용자 계정에서 관리자 메뉴가 노출되지 않는지를 실제 운영에서 함께 확인해야 한다.
- 대상자 생성 확인: `미실행`
- 기준: 신규 대상자 생성 성공, `clientNo` 생성, 상세 화면 또는 응답 정상 여부를 실제 운영에서 확인해야 한다.
- 최소 2종 세션 저장 확인: `미실행`
- 기준: 예: `PHQ-9 + GAD-7` 조합 등 최소 2종 척도 세션 저장 성공과 `sessionNo` 생성을 실제 운영에서 확인해야 한다.
- 세션 상세 확인: `미실행`
- 기준: 방금 저장한 세션 상세 화면이 정상 열리고 총점, 판정, 경고, 문항 응답 표시가 정상인지 실제 운영에서 확인해야 한다.
- `print view` 확인: `미실행`
- 기준: 같은 세션의 `print view` 가 정상 표시되고 인쇄 화면까지 확인되며 세션 메모는 포함되지 않는지 실제 운영에서 확인해야 한다.
- `statistics` 확인: `미실행`
- 기준: `statistics/summary`, `statistics/scales`, `statistics/alerts` 가 모두 정상 응답하고 최신 저장 데이터가 집계에 반영되는지 실제 운영에서 확인해야 한다.
- `CSV export` 확인: `미실행`
- 기준: 관리자 계정 기준 다운로드 성공, 빈 파일 아님, 인코딩 또는 컬럼 깨짐 없음을 실제 운영에서 확인해야 한다.
- 활동 로그 확인: `미실행`
- 기준: 로그인, 대상자 생성, 세션 저장, `print view`, export, 백업 등 주요 행위가 최신순으로 남는지 실제 운영에서 확인해야 한다.
- 수동 백업 확인: `미실행`
- 기준: 운영에서 사용하기로 한 수동 백업 절차 1회 확인, 백업 이력과 실제 산출물 또는 운영 승인 대체 수단이 일치하는지 실제 운영에서 확인해야 한다.
- 메모: 이번 문서는 로컬 검증 스택 증빙만 다루며 실제 운영 서버 스모크 테스트 결과는 포함하지 않는다. 위 항목은 [docs/15-go-live-checklist.md](../15-go-live-checklist.md) 6장의 최소 운영 스모크 범위에 맞춰 정렬했으며, 현재 상태는 모두 `미실행` 이다.

### 4.4 Docker 운영 체크 결과

- `.env` 작성 및 보관 위치 확인: `미실행`
- 외부 MariaDB 생성 확인: `미실행`
- `schema.sql` 적용 확인: `미실행`
- 초기 관리자 계정 준비 확인: `미실행`
- `docker compose up -d` 실행 결과: `미실행`
- `docker compose ps` 확인 결과: `미실행`
- `docker compose restart` 확인 결과: `미실행`
- 재부팅 후 재기동 확인 결과: `미실행`
- 로그/임시파일/백업 host path 확인: `미실행`
- 장애 시 1차 확인 명령 실행 여부: `미실행`
- 메모: 실제 운영 배포를 수행할 때는 실제 운영 서버에서 다시 증빙해야 한다.

---

## 5. 롤백/중지 판단 결과

- 실제 운영 롤백 판단 대상 여부: `미대상`
- 실제 운영 배포 중지 사유: `미기록`
- readiness 최종 판단: `READY`
- 비고: 로컬 검증 기준에서는 Docker 운영 설정 반영과 재시작 동작을 확인했고, 실제 운영 배포는 수행하지 않았으므로 롤백 판단은 미대상이다.

---

## 6. 후속 조치

- 즉시 수정 필요 항목: `없음`
- 차기 배포 전 보강 항목: `실제 운영 DB schema.sql 적용`, `실제 운영 초기 관리자 준비`, `실제 운영 서버 반영 기록`
- 문서 업데이트 필요 항목: `실제 운영 배포 수행 시 이 문서의 4번 섹션 갱신`
- 담당자: `미기록`
- 완료 목표일: `미기록`
