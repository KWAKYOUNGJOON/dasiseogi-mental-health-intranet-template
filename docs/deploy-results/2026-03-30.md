# Deploy Result

- 일자별 결과 문서는 `docs/deploy-results/YYYY-MM-DD.md` 형식으로 생성해 보관한다.
- 동일 문서 안에서 `검증 환경 결과` 와 `실제 운영 배포 결과` 를 반드시 분리 기록한다.
- 현재 [docs/12-release-readiness.md](../12-release-readiness.md) 기준 판정이 `READY` 여도, 실제 운영 배포 완료 여부는 이 문서에서 별도로 기록한다.
- 실제 운영 배포를 수행하지 않았다면 관련 항목은 `미실행` 또는 `미기록` 으로 남긴다.

## 1. 기본 정보

- 기준 일자: `2026-03-30`
- release readiness 기준 판정: `READY`
- 배포 버전: `미기록`
- 관련 커밋/브랜치: `미기록`
- 배포 담당자: `미기록`
- 검수 담당자: `미기록`
- 관련 readiness 문서:
  - [docs/12-release-readiness.md](../12-release-readiness.md)
  - [docs/13-pre-deploy-runbook.md](../13-pre-deploy-runbook.md)
  - [docs/15-go-live-checklist.md](../15-go-live-checklist.md)
- 비고: `2026-03-30` 기준 readiness 판정은 `READY` 이며, 이는 실제 운영 배포 완료를 뜻하지 않는다.

---

## 2. 현재 판정

- readiness 현재 판정: `READY`
- 검증 환경 결과 반영 여부: `반영 완료`
- 실제 운영 배포 진행 여부: `미실행`
- 비고: `READY` 와 실제 운영 배포 `미실행` 은 동시에 성립할 수 있다.

---

## 3. 검증 환경 결과

### 3.1 검증 개요

- 검증 수행 여부: `예`
- 검증 환경: `로컬 MariaDB 검증 환경`
- 검증 시작 시각: `2026-03-30 00:38 KST`
- 검증 완료 시각: `2026-03-30 00:41 KST`
- 검증 요약: `MariaDB/MySQL 실검증 완료, 검증 환경에서 운영 절차 기준 DB_DUMP 실제 성공 확인, 오늘 검증 체크리스트 1-12 성공 기준 반영으로 READY 판정 유지`

### 3.2 MariaDB/MySQL 실검증 결과

- 실행 일시: `2026-03-30 00:41 KST`
- 실행 환경: `Windows + Docker Desktop 29.3.1 / Docker Desktop 4.66.1 환경`
- 실행 명령:

```powershell
cd backend
.\gradlew.bat mariaDbTest --rerun-tasks
```

- 결과:
  - [x] 성공
  - [ ] 실패
  - [ ] skip
- `MariaDbCompatibilityTest` 수행 건수: `4`
- `MariaDbCompatibilityTest` skip 건수: `0`
- 확인 파일: `backend/build/test-results/mariaDbTest/TEST-com.dasisuhgi.mentalhealth.MariaDbCompatibilityTest.xml`
- 비고: `BUILD SUCCESSFUL`, 수행 확인은 `assessmentRecordsProjectionFilterAndPageableWorkOnMariaDb`, `statisticsSummaryAndScalesAggregateCorrectlyOnMariaDb`, `statisticsAlertsFiltersByScaleCodeAndAlertTypeOnMariaDb`, `loginWorksAgainstMariaDbContainer` 기준으로 기록했다.

### 3.3 DB_DUMP 수동 백업 결과

- 실행 일시: `2026-03-30 00:38 KST`
- 실행 환경: `로컬 MariaDB 검증 환경`
- 실행 계정: `관리자A`
- 실행 방식:
  - [ ] 관리자 화면
  - [x] API
- 입력 사유: `배포 직전 수동 백업`
- 응답 상태: `SUCCESS`
- `backupMethod`: `DB_DUMP`
- `datasourceType`: `MARIADB`
- `preflightSummary`: `datasource=MARIADB, preferred=DB_DUMP, dumpCommand=D:\\dasiseogi-mental-health-intranet-template\\tmp\\mariadb\\dist\\MariaDB-12.2\\bin\\mariadb-dump.exe, dumpAvailable=true, fallback=-`
- 생성 파일명: `backup-20260330-003830-db-dump.sql`
- 생성 파일경로: `d:\dasiseogi-mental-health-intranet-template\local-backups\go-live-mariadb-check\backup-20260330-003830-db-dump.sql`
- 파일 존재 확인:
  - [x] 예
  - [ ] 아니오
- `backup_histories` 최신 이력 확인:
  - [x] 예
  - [ ] 아니오
- 비고: 최신 이력은 `backupId=2`, `status=SUCCESS`, `backupMethod=DB_DUMP`, 실행자는 `관리자A`, 파일 크기는 `17679 bytes` 로 기록되어 있다. PowerShell 환경변수 주입과 Gradle daemon 조합으로 H2 fallback 이 섞일 수 있어, 이번 검증은 `bootRun --args` 로 datasource/backup 설정을 직접 넘겨 MariaDB 를 고정했다.

### 3.4 오늘 검증 체크리스트 결과

- `health` endpoint 확인
  - [x] 성공
  - [ ] 실패
- 관리자 로그인 확인
  - [x] 성공
  - [ ] 실패
- 대상자 등록 확인
  - [x] 성공
  - [ ] 실패
- 최소 2종 멀티 척도 세션 저장 확인
  - [x] 성공
  - [ ] 실패
- 세션 상세 확인
  - [x] 성공
  - [ ] 실패
- `print view` 확인
  - [x] 성공
  - [ ] 실패
- `statistics/summary` 확인
  - [x] 성공
  - [ ] 실패
- `statistics/scales` 확인
  - [x] 성공
  - [ ] 실패
- `statistics/alerts` 확인
  - [x] 성공
  - [ ] 실패
- `CSV export` 확인
  - [x] 성공
  - [ ] 실패
- 활동 로그 확인
  - [x] 성공
  - [ ] 실패
- 수동 백업 이력 확인
  - [x] 성공
  - [ ] 실패
- 메모: `health` 확인 결과는 `HTTP 200`, `status=UP`, `appStatus=UP`, `dbStatus=UP`, `scaleRegistryStatus=UP`, `loadedScaleCount=9` 이다. 세부 실행 결과는 [docs/15-go-live-checklist.md](../15-go-live-checklist.md) 의 `3. 검증 환경 실행 체크리스트` 기록을 따른다.

---

## 4. 실제 운영 배포 결과

- 실제 운영 배포 진행 여부: `미실행`

### 4.1 배포 수행 메타

- 배포 시작 시각: `미실행`
- 배포 완료 시각: `미실행`
- 배포 대상 환경: `미기록`

### 4.2 배포 수행 기록

- 백엔드 반영 결과: `미실행`
- 프론트 반영 결과: `미실행`
- 설정 파일 반영 결과: `미실행`
- 척도 JSON 반영 결과: `미실행`
- 배포 중 오류: `미기록`

### 4.3 배포 후 스모크 테스트 결과

- 관리자 로그인: `미실행`
- 대상자 등록: `미실행`
- 최소 2종 멀티 척도 세션 저장: `미실행`
- 세션 상세 조회: `미실행`
- `print view`: `미실행`
- `statistics/summary`: `미실행`
- `statistics/scales`: `미실행`
- `statistics/alerts`: `미실행`
- `CSV export`: `미실행`
- 활동 로그 조회: `미실행`
- 수동 백업 이력 조회: `미실행`
- `/api/v1/health` 재확인: `미실행`
- 메모: `실제 운영 배포를 수행하지 않았으므로 관련 항목은 미실행 또는 미기록으로 남긴다.`

---

## 5. 롤백/중지 판단 결과

- 실제 운영 롤백 판단 대상 여부: `미대상`
- 실제 운영 배포 중지 사유: `미기록`
- readiness 최종 판단: `READY`
- 비고: `검증 환경 기준 READY 판정을 유지하며, 실제 운영 배포를 수행하지 않았으므로 롤백 판단은 미대상으로 기록한다.`

---

## 6. 후속 조치

- 즉시 수정 필요 항목: `없음`
- 차기 배포 전 보강 항목: [docs/12-release-readiness.md](../12-release-readiness.md) 의 `실제 운영 배포 전 최종 선행 조치` 수행
- 문서 업데이트 필요 항목: `실제 운영 배포 수행 시 이 문서의 실제 운영 배포 결과 갱신`
- 담당자: `미기록`
- 완료 목표일: `미기록`
