# Deploy Result

- 일자별 결과 문서는 `docs/deploy-results/YYYY-MM-DD.md` 형식으로 생성해 보관한다.
- 동일 문서 안에서 `검증 환경 결과` 와 `실제 운영 배포 결과` 를 반드시 분리 기록한다.
- 현재 [docs/12-release-readiness.md](../12-release-readiness.md) 기준 판정이 `READY` 여도, 실제 운영 배포 완료 여부는 이 문서에서 별도로 기록한다.
- 실제 운영 배포를 수행하지 않았다면 관련 항목은 `미실행` 또는 `미기록` 으로 남긴다.

## 1. 기본 정보

- 기준 일자: `2026-03-31`
- release readiness 기준 판정: `READY`
- 배포 버전: `미기록`
- 관련 커밋/브랜치: `미기록`
- 배포 담당자: `미기록`
- 검수 담당자: `미기록`
- 관련 readiness 문서:
  - [docs/12-release-readiness.md](../12-release-readiness.md)
  - [docs/13-pre-deploy-runbook.md](../13-pre-deploy-runbook.md)
  - [docs/15-go-live-checklist.md](../15-go-live-checklist.md)
- 비고: `2026-03-31` 기준 검증 환경 결과는 `CoreWorkflowIntegrationTest` 기대값 최소 수정 후 `backend test`, `frontend build` 성공 증빙만 반영했고, 실제 운영 배포는 수행하지 않았다.

---

## 2. 현재 판정

- readiness 현재 판정: `READY`
- 검증 환경 결과 반영 여부: `반영 완료`
- 실제 운영 배포 진행 여부: `미실행`
- 비고: `READY` 는 현재 증빙 범위 기준 배포 가능 판정이며, 이번 기록은 build/test 성공 반영만 다룬다.

---

## 3. 검증 환경 결과

### 3.1 검증 개요

- 검증 수행 여부: `예`
- 검증 환경: `로컬 개발/검증 환경`
- 검증 시작 시각: `미기록`
- 검증 완료 시각: `미기록`
- 검증 요약: `CoreWorkflowIntegrationTest` 기대값만 최소 수정한 뒤 `backend` `.\gradlew.bat test` 와 `frontend` `npm run build` 가 모두 성공했다. 실제 운영 배포는 수행하지 않았다.

### 3.2 백엔드 테스트 결과

- 실행 일시: `2026-03-31`
- 실행 환경: `backend`
- 실행 명령:

```powershell
cd backend
.\gradlew.bat test
```

- 결과:
  - [x] 성공
  - [ ] 실패
  - [ ] skip
- Gradle 결과: `BUILD SUCCESSFUL`
- 테스트 요약: `67 tests completed`
- 실패 건수: `0`
- 비고: `CoreWorkflowIntegrationTest` 의 식별자 기대값만 최소 수정한 뒤 재실행한 결과다.

### 3.3 프론트엔드 빌드 결과

- 실행 일시: `2026-03-31`
- 실행 환경: `frontend`
- 실행 명령:

```powershell
cd frontend
npm run build
```

- 결과:
  - [x] 성공
  - [ ] 실패
  - [ ] skip
- 빌드 요약: `tsc -b && vite build` 통과
- 비고: 프론트 빌드 성공 증빙만 기록하며, 실제 운영 정적 파일 반영은 수행하지 않았다.

### 3.4 오늘 검증 체크리스트 결과

- `backend`: `.\gradlew.bat test`
  - [x] 성공
  - [ ] 실패
- `frontend`: `npm run build`
  - [x] 성공
  - [ ] 실패
- 실제 운영 배포 수행
  - [ ] 성공
  - [x] 미실행
- 메모: 이번 문서는 build/test 성공 반영만 다루며, MariaDB 재검증, 수동 백업, 운영 서버 반영 결과는 포함하지 않는다.

---

## 4. 실제 운영 배포 결과

- 실제 운영 배포 진행 여부: `미실행`

### 4.1 배포 수행 메타

- 배포 시작 시각: `미실행`
- 배포 완료 시각: `미실행`
- 배포 대상 환경: `미기록`

### 4.2 배포 수행 기록

- 백엔드 반영 결과: `미실행`
- 프론트 반영 결과: `미실행`
- 설정 파일 반영 결과: `미실행`
- 척도 JSON 반영 결과: `미실행`
- 배포 중 오류: `미기록`

### 4.3 배포 후 스모크 테스트 결과

- 관리자 로그인: `미실행`
- 대상자 등록: `미실행`
- 최소 2종 멀티 척도 세션 저장: `미실행`
- 세션 상세 조회: `미실행`
- `print view`: `미실행`
- `statistics/summary`: `미실행`
- `statistics/scales`: `미실행`
- `statistics/alerts`: `미실행`
- `CSV export`: `미실행`
- 활동 로그 조회: `미실행`
- 수동 백업 이력 조회: `미실행`
- `/api/v1/health` 재확인: `미실행`
- 메모: `실제 운영 배포를 수행하지 않았으므로 관련 항목은 미실행 또는 미기록으로 유지한다.`

---

## 5. 롤백/중지 판단 결과

- 실제 운영 롤백 판단 대상 여부: `미대상`
- 실제 운영 배포 중지 사유: `미기록`
- readiness 최종 판단: `READY`
- 비고: `검증 환경 기준 READY 판정을 유지하며, 실제 운영 배포를 수행하지 않았으므로 롤백 판단은 미대상으로 기록한다.`

---

## 6. 후속 조치

- 즉시 수정 필요 항목: `없음`
- 차기 배포 전 보강 항목: `실제 운영 배포 전 백업, health, 운영 스모크 결과를 별도 기록`
- 문서 업데이트 필요 항목: `실제 운영 배포 수행 시 이 문서의 실제 운영 배포 결과 갱신`
- 담당자: `미기록`
- 완료 목표일: `미기록`
