services:
  backend:
    build:
      context: ./backend
    image: dasiseogi-backend:local
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
    image: dasiseogi-frontend:local
    depends_on:
      - backend
    ports:
      - "4173:4173"
